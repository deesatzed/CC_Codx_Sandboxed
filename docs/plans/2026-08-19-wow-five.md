# Five wow receipts — implementation plan

> Implement in this sandbox. TDD for Python. Live scripts for doctor/prove.

**Goal:** Ship hop receipts, sandbox proof, graph-first local inject, witness log, and `/local`.

**Architecture:** Router writes receipts and honors `.force-local`. Launchers + hooks append `witness.jsonl`. Shorthand optionally prefixes a compact subgraph. Two bash scripts produce human-readable tapes.

**Tech stack:** existing FastAPI router, Safehouse, pytest, no new services.

### Task 1: classify + receipts + force-local
### Task 2: graph compact + shorthand inject
### Task 3: witness append + hooks
### Task 4: router wiring + `/v1/doctor/receipt`
### Task 5: `doctor-receipt.sh`, `prove-sandbox.sh`, slash commands
### Task 6: README / BUILD / gitignore / tests green
